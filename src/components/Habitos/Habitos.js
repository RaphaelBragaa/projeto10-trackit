import Header from "../shared/Header"
import Menu from '../shared/Menu';

export default function Habitos({token}){
    console.log(token)
    return(
        <>
        <Header/>
        HABITOS
        <Menu/>
        </>
    )
}